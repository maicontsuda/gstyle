export const dynamic = 'force-dynamic';
import { NextResponse } from 'next/server'; import dbConnect from '@/lib/mongoose'; import CarModel from '@/models/Model'; import { authenticateToken, roleCheck } from '@/lib/authHelpers';  // PUBLIC: Puxar modelos ativos de uma montadora específica export async function GET(req: Request) {   try {     const { searchParams } = new URL(req.url);     const manufacturerId = searchParams.get('manufacturerId');      await dbConnect();      // Se o ManufacturerId for passado, filtramos, se não passa todos     const filter = manufacturerId ? { active: true, manufacturerId } : { active: true };     const models = await CarModel.find(filter).sort({ name: 1 });          return NextResponse.json(models);   } catch (error) {     return NextResponse.json({ error: 'Erro ao buscar modelos.' }, { status: 500 });   } }  // ADMIN: Criar novo Modelo export async function POST(req: Request) {   try {     const auth = await authenticateToken(req);     if (auth.error) return NextResponse.json({ error: auth.error }, { status: auth.status });          const roleAuth = roleCheck(auth.user, ['admin', 'dono', 'funcionario']);     if (roleAuth.error) return NextResponse.json({ error: roleAuth.error }, { status: roleAuth.status });      await dbConnect();     const data = await req.json();      const novoModelo = new CarModel(data);     await novoModelo.save();          return NextResponse.json(novoModelo, { status: 201 });   } catch (error: any) {     return NextResponse.json({ error: 'Erro ao criar modelo.' }, { status: 500 });   } }
