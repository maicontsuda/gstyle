export const dynamic = 'force-dynamic';
import { NextResponse } from 'next/server'; import dbConnect from '@/lib/mongoose'; import CarVersion from '@/models/Version'; import { authenticateToken, roleCheck } from '@/lib/authHelpers';  // PUBLIC: Puxar versões de um Modelo Específico export async function GET(req: Request) {   try {     const { searchParams } = new URL(req.url);     const modelId = searchParams.get('modelId');      await dbConnect();      const filter = modelId ? { active: true, modelId } : { active: true };     const versions = await CarVersion.find(filter).sort({ basePrice: 1 });          return NextResponse.json(versions);   } catch (error) {     return NextResponse.json({ error: 'Erro ao buscar versões.' }, { status: 500 });   } }  // ADMIN: Criar nova Versão do modelo export async function POST(req: Request) {   try {     const auth = await authenticateToken(req);     if (auth.error) return NextResponse.json({ error: auth.error }, { status: auth.status });          const roleAuth = roleCheck(auth.user, ['admin', 'dono', 'funcionario']);     if (roleAuth.error) return NextResponse.json({ error: roleAuth.error }, { status: roleAuth.status });      await dbConnect();     const data = await req.json();      const novaVersao = new CarVersion(data);     await novaVersao.save();          return NextResponse.json(novaVersao, { status: 201 });   } catch (error: any) {     return NextResponse.json({ error: 'Erro ao criar versão.' }, { status: 500 });   } }
